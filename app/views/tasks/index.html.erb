<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <% if task.completed%>
    [x]
    <% else %>
    [ ]
    <% end %>
    <%= link_to task.title, task_path(task) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a task", new_task_path %>


